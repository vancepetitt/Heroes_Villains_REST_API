# Heroes_Villains_REST_API
Heroes and Villains project to build a REST web API using Django REST framework for devCodeCamp.
Use "hero" and "villain" when querying in the URL to filter supers by super types.
